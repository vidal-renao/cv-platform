export const metadata = {
  title: 'Crear Cuenta',
  description: 'Crea tu cuenta en CV Platform y gestiona todos tus envíos desde un solo lugar.',
  robots: { index: false, follow: false },
};

export default function RegisterLayout({ children }) {
  return children;
}
